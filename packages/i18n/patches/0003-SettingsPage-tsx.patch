diff --git a/ui/src/pages/SettingsPage.tsx b/ui/src/pages/SettingsPage.tsx
index f3f4386..cba1d7e 100644
--- a/ui/src/pages/SettingsPage.tsx
+++ b/ui/src/pages/SettingsPage.tsx
@@ -6,9 +6,16 @@ import { ConfigSection, Field, inputClass } from '../components/form'
 import { useAutoSave } from '../hooks/useAutoSave'
 import { PageHeader } from '../components/PageHeader'
 import { PageLoading } from '../components/StateViews'
+import { useTranslation, type Locale } from '../i18n'
+
+const LANGUAGES: { value: Locale; label: string }[] = [
+  { value: 'en', label: 'English' },
+  { value: 'zh', label: '中文' },
+]
 
 export function SettingsPage() {
   const [config, setConfig] = useState<AppConfig | null>(null)
+  const { t, locale, setLocale } = useTranslation()
 
   useEffect(() => {
     api.config.load().then(setConfig).catch(() => {})
@@ -16,22 +23,40 @@ export function SettingsPage() {
 
   return (
     <div className="flex flex-col flex-1 min-h-0">
-      <PageHeader title="Settings" />
+      <PageHeader
+        title={t.settings.title}
+        right={
+          <div className="flex items-center gap-2">
+            <span className="text-sm text-text-muted">{t.settings.language}:</span>
+            <select
+              value={locale}
+              onChange={(e) => setLocale(e.target.value as Locale)}
+              className="bg-bg text-text border border-border rounded px-2 py-1 text-sm outline-none focus:border-accent cursor-pointer"
+            >
+              {LANGUAGES.map((lang) => (
+                <option key={lang.value} value={lang.value}>
+                  {lang.label}
+                </option>
+              ))}
+            </select>
+          </div>
+        }
+      />
 
       {config ? (
       <div className="flex-1 overflow-y-auto px-4 md:px-8 py-6">
           <div className="max-w-[880px] mx-auto">
             {/* Agent */}
-            <ConfigSection title="Agent" description="Controls file-system and tool permissions for the AI. Changes apply on the next request.">
+            <ConfigSection title={t.settings.agent} description={t.settings.agentDesc}>
               <div className="flex items-center justify-between gap-4 py-1">
                 <div className="flex-1">
                   <span className="text-sm font-medium text-text">
-                    Evolution Mode
+                    {t.settings.evolutionMode}
                   </span>
                   <p className="text-[12px] text-text-muted mt-0.5 leading-relaxed">
                     {config.agent?.evolutionMode
-                      ? 'Full project access — AI can modify source code'
-                      : 'Sandbox mode — AI can only edit data/brain/'}
+                      ? t.settings.evolutionModeDesc.on
+                      : t.settings.evolutionModeDesc.off}
                   </p>
                 </div>
                 <Toggle
@@ -49,7 +74,7 @@ export function SettingsPage() {
             </ConfigSection>
 
             {/* Compaction */}
-            <ConfigSection title="Compaction" description="Context window management. When conversation size approaches Max Context minus Max Output tokens, older messages are automatically summarized to free up space.">
+            <ConfigSection title={t.settings.compaction} description={t.settings.compactionDesc}>
               <CompactionForm config={config} />
             </ConfigSection>
           </div>
@@ -66,6 +91,7 @@ export function SettingsPage() {
 function CompactionForm({ config }: { config: AppConfig }) {
   const [ctx, setCtx] = useState(String(config.compaction?.maxContextTokens || ''))
   const [out, setOut] = useState(String(config.compaction?.maxOutputTokens || ''))
+  const { t } = useTranslation()
 
   const data = useMemo(
     () => ({ maxContextTokens: Number(ctx), maxOutputTokens: Number(out) }),
@@ -80,14 +106,13 @@ function CompactionForm({ config }: { config: AppConfig }) {
 
   return (
     <>
-      <Field label="Max Context Tokens">
+      <Field label={t.settings.maxContextTokens}>
         <input className={inputClass} type="number" step={1000} value={ctx} onChange={(e) => setCtx(e.target.value)} />
       </Field>
-      <Field label="Max Output Tokens">
+      <Field label={t.settings.maxOutputTokens}>
         <input className={inputClass} type="number" step={1000} value={out} onChange={(e) => setOut(e.target.value)} />
       </Field>
       <SaveIndicator status={status} onRetry={retry} />
     </>
   )
 }
-
